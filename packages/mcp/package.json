{
  "name": "@rundown-org/mcp",
  "version": "1.0.0",
  "description": "MCP server for Rundown runbook execution",
  "type": "module",
  "main": "dist/index.js",
  "bin": {
    "rundown-mcp": "./dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "check:types": "tsc --noEmit -p tsconfig.test.json",
    "test": "jest",
    "test:unit": "jest --maxWorkers=2",
    "test:coverage": "jest --coverage",
    "analyze:complexity": "fta src --score-cap 54"
  },
  "engines": {
    "node": ">=24.0.0"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "@rundown-org/core": "*",
    "zod": "^4.5.4"
  },
  "devDependencies": {
    "@jest/globals": "^30.5.0",
    "@types/jest": "^30.0.0",
    "@types/node": "^26.4.0",
    "fta-cli": "3.0.1",
    "jest": "^30.5.0",
    "jest-mock": "^30.5.0",
    "ts-jest": "^29.4.11",
    "typescript": "^6.0.3"
  }
}

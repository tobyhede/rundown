version: 1
name: plugin-runbooks
description: Structural coverage for plugin runbooks

cases:
  # --- write-plan: happy path (all 10 steps pass) ---

  write-plan-all-pass:
    description: All steps pass through full pipeline — completes
    file: runbooks/planning/write-plan.runbook.md
    commands:
      - rd run --prompted write-plan.runbook.md
      - rd pass   # 1. Invoke the Writing Plans skill → CONTINUE
      - rd pass   # 2. Review the schema → CONTINUE
      - rd pass   # 3. Check the Scope → CONTINUE
      - rd pass   # 4. Gather Requirements → CONTINUE
      - rd pass   # 5. Research Codebase → CONTINUE
      - rd pass   # 6. Map File Structure → CONTINUE
      - rd pass   # 7. Output Path → CONTINUE
      - rd pass   # 8. Write the plan → CONTINUE
      - rd pass   # 9. Check Schema → CONTINUE
      - rd pass   # 10. Verify Plan Structure → COMPLETE
    result: COMPLETE

  # --- write-plan: early termination ---

  write-plan-fail-at-skill-check:
    description: Skill invocation fails at step 1 — stops
    file: runbooks/planning/write-plan.runbook.md
    commands:
      - rd run --prompted write-plan.runbook.md
      - rd fail   # 1. Invoke the Writing Plans skill → STOP
    result: STOP

  write-plan-fail-at-write:
    description: Plan writing fails at step 8 — stops
    file: runbooks/planning/write-plan.runbook.md
    commands:
      - rd run --prompted write-plan.runbook.md
      - rd pass   # 1. Invoke the Writing Plans skill → CONTINUE
      - rd pass   # 2. Review the schema → CONTINUE
      - rd pass   # 3. Check the Scope → CONTINUE
      - rd pass   # 4. Gather Requirements → CONTINUE
      - rd pass   # 5. Research Codebase → CONTINUE
      - rd pass   # 6. Map File Structure → CONTINUE
      - rd pass   # 7. Output Path → CONTINUE
      - rd fail   # 8. Write the plan → STOP
    result: STOP

  write-plan-schema-fail-then-pass:
    description: Schema check fails, GOTO 8 to revise, then passes — completes
    file: runbooks/planning/write-plan.runbook.md
    commands:
      - rd run --prompted write-plan.runbook.md
      - rd pass   # 1. Invoke the Writing Plans skill → CONTINUE
      - rd pass   # 2. Review the schema → CONTINUE
      - rd pass   # 3. Check the Scope → CONTINUE
      - rd pass   # 4. Gather Requirements → CONTINUE
      - rd pass   # 5. Research Codebase → CONTINUE
      - rd pass   # 6. Map File Structure → CONTINUE
      - rd pass   # 7. Output Path → CONTINUE
      - rd pass   # 8. Write the plan → CONTINUE
      - rd fail   # 9. Check Schema → GOTO 8
      - rd pass   # 8. Write the plan (revision) → CONTINUE
      - rd pass   # 9. Check Schema (retry) → CONTINUE
      - rd pass   # 10. Verify Plan Structure → COMPLETE
    result: COMPLETE

  write-plan-verify-fail-then-pass:
    description: Verification fails, GOTO 8 to revise, then passes — completes
    file: runbooks/planning/write-plan.runbook.md
    commands:
      - rd run --prompted write-plan.runbook.md
      - rd pass   # 1. Invoke the Writing Plans skill → CONTINUE
      - rd pass   # 2. Review the schema → CONTINUE
      - rd pass   # 3. Check the Scope → CONTINUE
      - rd pass   # 4. Gather Requirements → CONTINUE
      - rd pass   # 5. Research Codebase → CONTINUE
      - rd pass   # 6. Map File Structure → CONTINUE
      - rd pass   # 7. Output Path → CONTINUE
      - rd pass   # 8. Write the plan → CONTINUE
      - rd pass   # 9. Check Schema → CONTINUE
      - rd fail   # 10. Verify Plan Structure → GOTO 8
      - rd pass   # 8. Write the plan (revision) → CONTINUE
      - rd pass   # 9. Check Schema (retry) → CONTINUE
      - rd pass   # 10. Verify Plan Structure (retry) → COMPLETE
    result: COMPLETE

  # --- review-plan-technical-accuracy: 5 cases ---

  review-tech-all-pass:
    description: All steps pass through full pipeline — completes
    file: runbooks/planning/review/review-plan-technical-accuracy.runbook.md
    commands:
      - rd run --prompted review-plan-technical-accuracy.runbook.md
      - rd pass   # 1. Find plan → CONTINUE
      - rd pass   # 2. Read the output schema → CONTINUE
      - rd pass   # 3. Is the plan technically accurate? → CONTINUE
      - rd pass   # 4. Write the review → CONTINUE
      - rd pass   # 5. Check Schema → COMPLETE
    result: COMPLETE

  review-tech-find-fail:
    description: Plan not found at step 1 — stops
    file: runbooks/planning/review/review-plan-technical-accuracy.runbook.md
    commands:
      - rd run --prompted review-plan-technical-accuracy.runbook.md
      - rd fail   # 1. Find plan → STOP
    result: STOP

  review-tech-content-fail-continues:
    description: Step 3 judgement fails but FAIL CONTINUE proceeds — completes
    file: runbooks/planning/review/review-plan-technical-accuracy.runbook.md
    commands:
      - rd run --prompted review-plan-technical-accuracy.runbook.md
      - rd pass   # 1. Find plan → CONTINUE
      - rd pass   # 2. Read the output schema → CONTINUE
      - rd fail   # 3. Is the plan technically accurate? → CONTINUE (FAIL CONTINUE)
      - rd pass   # 4. Write the review → CONTINUE
      - rd pass   # 5. Check Schema → COMPLETE
    result: COMPLETE

  review-tech-write-fail:
    description: Write review fails at step 4 — stops
    file: runbooks/planning/review/review-plan-technical-accuracy.runbook.md
    commands:
      - rd run --prompted review-plan-technical-accuracy.runbook.md
      - rd pass   # 1. Find plan → CONTINUE
      - rd pass   # 2. Read the output schema → CONTINUE
      - rd pass   # 3. Is the plan technically accurate? → CONTINUE
      - rd fail   # 4. Write the review → STOP
    result: STOP

  review-tech-schema-fail-then-pass:
    description: Schema check fails, GOTO 4 to revise, then passes — completes
    file: runbooks/planning/review/review-plan-technical-accuracy.runbook.md
    commands:
      - rd run --prompted review-plan-technical-accuracy.runbook.md
      - rd pass   # 1. Find plan → CONTINUE
      - rd pass   # 2. Read the output schema → CONTINUE
      - rd pass   # 3. Is the plan technically accurate? → CONTINUE
      - rd pass   # 4. Write the review → CONTINUE
      - rd fail   # 5. Check Schema → GOTO 4
      - rd pass   # 4. Write the review (revision) → CONTINUE
      - rd pass   # 5. Check Schema (retry) → COMPLETE
    result: COMPLETE

  # --- review-plan-structural-integrity: 5 cases ---

  review-struct-all-pass:
    description: All steps pass through full pipeline — completes
    file: runbooks/planning/review/review-plan-structural-integrity.runbook.md
    commands:
      - rd run --prompted review-plan-structural-integrity.runbook.md
      - rd pass   # 1. Find plan → CONTINUE
      - rd pass   # 2. Read the output schema → CONTINUE
      - rd pass   # 3. Is the plan structurally sound? → CONTINUE
      - rd pass   # 4. Write the review → CONTINUE
      - rd pass   # 5. Check Schema → COMPLETE
    result: COMPLETE

  review-struct-find-fail:
    description: Plan not found at step 1 — stops
    file: runbooks/planning/review/review-plan-structural-integrity.runbook.md
    commands:
      - rd run --prompted review-plan-structural-integrity.runbook.md
      - rd fail   # 1. Find plan → STOP
    result: STOP

  review-struct-content-fail-continues:
    description: Step 3 judgement fails but FAIL CONTINUE proceeds — completes
    file: runbooks/planning/review/review-plan-structural-integrity.runbook.md
    commands:
      - rd run --prompted review-plan-structural-integrity.runbook.md
      - rd pass   # 1. Find plan → CONTINUE
      - rd pass   # 2. Read the output schema → CONTINUE
      - rd fail   # 3. Is the plan structurally sound? → CONTINUE (FAIL CONTINUE)
      - rd pass   # 4. Write the review → CONTINUE
      - rd pass   # 5. Check Schema → COMPLETE
    result: COMPLETE

  review-struct-write-fail:
    description: Write review fails at step 4 — stops
    file: runbooks/planning/review/review-plan-structural-integrity.runbook.md
    commands:
      - rd run --prompted review-plan-structural-integrity.runbook.md
      - rd pass   # 1. Find plan → CONTINUE
      - rd pass   # 2. Read the output schema → CONTINUE
      - rd pass   # 3. Is the plan structurally sound? → CONTINUE
      - rd fail   # 4. Write the review → STOP
    result: STOP

  review-struct-schema-fail-then-pass:
    description: Schema check fails, GOTO 4 to revise, then passes — completes
    file: runbooks/planning/review/review-plan-structural-integrity.runbook.md
    commands:
      - rd run --prompted review-plan-structural-integrity.runbook.md
      - rd pass   # 1. Find plan → CONTINUE
      - rd pass   # 2. Read the output schema → CONTINUE
      - rd pass   # 3. Is the plan structurally sound? → CONTINUE
      - rd pass   # 4. Write the review → CONTINUE
      - rd fail   # 5. Check Schema → GOTO 4
      - rd pass   # 4. Write the review (revision) → CONTINUE
      - rd pass   # 5. Check Schema (retry) → COMPLETE
    result: COMPLETE

  # --- review-plan-build-runtime: 5 cases ---

  review-build-all-pass:
    description: All steps pass through full pipeline — completes
    file: runbooks/planning/review/review-plan-build-runtime.runbook.md
    commands:
      - rd run --prompted review-plan-build-runtime.runbook.md
      - rd pass   # 1. Find plan → CONTINUE
      - rd pass   # 2. Read the output schema → CONTINUE
      - rd pass   # 3. Are build and runtime concerns addressed? → CONTINUE
      - rd pass   # 4. Write the review → CONTINUE
      - rd pass   # 5. Check Schema → COMPLETE
    result: COMPLETE

  review-build-find-fail:
    description: Plan not found at step 1 — stops
    file: runbooks/planning/review/review-plan-build-runtime.runbook.md
    commands:
      - rd run --prompted review-plan-build-runtime.runbook.md
      - rd fail   # 1. Find plan → STOP
    result: STOP

  review-build-content-fail-continues:
    description: Step 3 judgement fails but FAIL CONTINUE proceeds — completes
    file: runbooks/planning/review/review-plan-build-runtime.runbook.md
    commands:
      - rd run --prompted review-plan-build-runtime.runbook.md
      - rd pass   # 1. Find plan → CONTINUE
      - rd pass   # 2. Read the output schema → CONTINUE
      - rd fail   # 3. Are build and runtime concerns addressed? → CONTINUE (FAIL CONTINUE)
      - rd pass   # 4. Write the review → CONTINUE
      - rd pass   # 5. Check Schema → COMPLETE
    result: COMPLETE

  review-build-write-fail:
    description: Write review fails at step 4 — stops
    file: runbooks/planning/review/review-plan-build-runtime.runbook.md
    commands:
      - rd run --prompted review-plan-build-runtime.runbook.md
      - rd pass   # 1. Find plan → CONTINUE
      - rd pass   # 2. Read the output schema → CONTINUE
      - rd pass   # 3. Are build and runtime concerns addressed? → CONTINUE
      - rd fail   # 4. Write the review → STOP
    result: STOP

  review-build-schema-fail-then-pass:
    description: Schema check fails, GOTO 4 to revise, then passes — completes
    file: runbooks/planning/review/review-plan-build-runtime.runbook.md
    commands:
      - rd run --prompted review-plan-build-runtime.runbook.md
      - rd pass   # 1. Find plan → CONTINUE
      - rd pass   # 2. Read the output schema → CONTINUE
      - rd pass   # 3. Are build and runtime concerns addressed? → CONTINUE
      - rd pass   # 4. Write the review → CONTINUE
      - rd fail   # 5. Check Schema → GOTO 4
      - rd pass   # 4. Write the review (revision) → CONTINUE
      - rd pass   # 5. Check Schema (retry) → COMPLETE
    result: COMPLETE

  # --- review-plan-risk-safety: 5 cases ---

  review-risk-all-pass:
    description: All steps pass through full pipeline — completes
    file: runbooks/planning/review/review-plan-risk-safety.runbook.md
    commands:
      - rd run --prompted review-plan-risk-safety.runbook.md
      - rd pass   # 1. Find plan → CONTINUE
      - rd pass   # 2. Read the output schema → CONTINUE
      - rd pass   # 3. Is the plan safe and risk-free? → CONTINUE
      - rd pass   # 4. Write the review → CONTINUE
      - rd pass   # 5. Check Schema → COMPLETE
    result: COMPLETE

  review-risk-find-fail:
    description: Plan not found at step 1 — stops
    file: runbooks/planning/review/review-plan-risk-safety.runbook.md
    commands:
      - rd run --prompted review-plan-risk-safety.runbook.md
      - rd fail   # 1. Find plan → STOP
    result: STOP

  review-risk-content-fail-continues:
    description: Step 3 judgement fails but FAIL CONTINUE proceeds — completes
    file: runbooks/planning/review/review-plan-risk-safety.runbook.md
    commands:
      - rd run --prompted review-plan-risk-safety.runbook.md
      - rd pass   # 1. Find plan → CONTINUE
      - rd pass   # 2. Read the output schema → CONTINUE
      - rd fail   # 3. Is the plan safe and risk-free? → CONTINUE (FAIL CONTINUE)
      - rd pass   # 4. Write the review → CONTINUE
      - rd pass   # 5. Check Schema → COMPLETE
    result: COMPLETE

  review-risk-write-fail:
    description: Write review fails at step 4 — stops
    file: runbooks/planning/review/review-plan-risk-safety.runbook.md
    commands:
      - rd run --prompted review-plan-risk-safety.runbook.md
      - rd pass   # 1. Find plan → CONTINUE
      - rd pass   # 2. Read the output schema → CONTINUE
      - rd pass   # 3. Is the plan safe and risk-free? → CONTINUE
      - rd fail   # 4. Write the review → STOP
    result: STOP

  review-risk-schema-fail-then-pass:
    description: Schema check fails, GOTO 4 to revise, then passes — completes
    file: runbooks/planning/review/review-plan-risk-safety.runbook.md
    commands:
      - rd run --prompted review-plan-risk-safety.runbook.md
      - rd pass   # 1. Find plan → CONTINUE
      - rd pass   # 2. Read the output schema → CONTINUE
      - rd pass   # 3. Is the plan safe and risk-free? → CONTINUE
      - rd pass   # 4. Write the review → CONTINUE
      - rd fail   # 5. Check Schema → GOTO 4
      - rd pass   # 4. Write the review (revision) → CONTINUE
      - rd pass   # 5. Check Schema (retry) → COMPLETE
    result: COMPLETE

  # --- review-plan-collate: 5 cases ---

  collate-all-pass:
    description: All steps pass through full pipeline — completes
    file: runbooks/planning/review/review-plan-collate.runbook.md
    commands:
      - rd run --prompted review-plan-collate.runbook.md
      - rd pass   # 1. Find reviews → CONTINUE
      - rd pass   # 2. Read the output schema → CONTINUE
      - rd pass   # 3. Collate the review findings → CONTINUE
      - rd pass   # 4. Write the collated review → CONTINUE
      - rd pass   # 5. Check Schema → COMPLETE
    result: COMPLETE

  collate-no-reviews-found:
    description: rdpath find returns empty at step 1 — stops
    file: runbooks/planning/review/review-plan-collate.runbook.md
    commands:
      - rd run --prompted review-plan-collate.runbook.md
      - rd fail   # 1. Find reviews → STOP
    result: STOP

  collate-merge-fail:
    description: Merge fails at step 3 — stops (collator step 3 is FAIL STOP, unlike reviewers)
    file: runbooks/planning/review/review-plan-collate.runbook.md
    commands:
      - rd run --prompted review-plan-collate.runbook.md
      - rd pass   # 1. Find reviews → CONTINUE
      - rd pass   # 2. Read the output schema → CONTINUE
      - rd fail   # 3. Collate the review findings → STOP
    result: STOP

  collate-write-fail:
    description: Write collated review fails at step 4 — stops
    file: runbooks/planning/review/review-plan-collate.runbook.md
    commands:
      - rd run --prompted review-plan-collate.runbook.md
      - rd pass   # 1. Find reviews → CONTINUE
      - rd pass   # 2. Read the output schema → CONTINUE
      - rd pass   # 3. Collate the review findings → CONTINUE
      - rd fail   # 4. Write the collated review → STOP
    result: STOP

  collate-schema-fail-then-pass:
    description: Schema check fails, GOTO 4 to revise, then passes — completes
    file: runbooks/planning/review/review-plan-collate.runbook.md
    commands:
      - rd run --prompted review-plan-collate.runbook.md
      - rd pass   # 1. Find reviews → CONTINUE
      - rd pass   # 2. Read the output schema → CONTINUE
      - rd pass   # 3. Collate the review findings → CONTINUE
      - rd pass   # 4. Write the collated review → CONTINUE
      - rd fail   # 5. Check Schema → GOTO 4
      - rd pass   # 4. Write the collated review (revision) → CONTINUE
      - rd pass   # 5. Check Schema (retry) → COMPLETE
    result: COMPLETE

---
const features = [
  {
    title: 'Markdown Runbooks',
    icon: '📄',
    description: 'Human-readable runbook definitions in familiar .runbook.md files',
  },
  {
    title: 'State Persistence',
    icon: '💾',
    description: 'Runbook progress survives context clears and session restarts',
  },
  {
    title: 'XState Compilation',
    icon: '⚙️',
    description: 'Runbooks compile to state machines for reliable execution',
  },
  {
    title: 'CLI Control',
    icon: '💻',
    description: 'Simple commands to run, pass, fail, and navigate runbooks',
  },
];
---

<section id="features" class="py-24 px-4">
  <div class="container mx-auto max-w-6xl">
    <h2 class="text-3xl md:text-4xl font-bold text-center mb-16">
      <span class="text-primary">Features</span>
    </h2>

    <div class="grid md:grid-cols-2 gap-8">
      {features.map((feature) => (
        <div
          class="p-6 rounded-xl bg-card border border-border hover:border-primary transition-all duration-300"
        >
          <div class="text-4xl mb-4">{feature.icon}</div>
          <h3 class="text-xl font-bold text-gray-900 dark:text-white mb-2">{feature.title}</h3>
          <p class="text-gray-600 dark:text-gray-400">{feature.description}</p>
        </div>
      ))}
    </div>
  </div>
</section>

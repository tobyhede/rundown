{
  "name": "rundown-monorepo",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*",
    "site"
  ],
  "scripts": {
    "build": "npm run build -w packages/parser && npm run build -w packages/core && npm run build -w packages/cli && npm run build -w packages/claude-code-plugin",
    "build:site": "npm run build -w site",
    "build:all": "npm run build && npm run build -w site",
    "test": "npm run test -w packages/parser && npm run test -w packages/core && npm run test -w packages/cli && npm run test -w packages/claude-code-plugin",
    "test:pw": "npm run test -w site",
    "test:coverage": "npm run test:coverage -w packages/parser && npm run test:coverage -w packages/core && npm run test:coverage -w packages/cli && npm run test:coverage -w packages/claude-code-plugin",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "cli": "node packages/cli/dist/cli.js",
    "changeset": "changeset",
    "version": "changeset version",
    "release": "changeset publish"
  },
  "devDependencies": {
    "@changesets/cli": "^2.29.8",
    "eslint": "^9.17.0",
    "globals": "^16.0.0",
    "typescript-eslint": "^8.19.1"
  }
}

# Rundown Specification

> **This file has moved.** The canonical location is [docs/spec/language.md](spec/language.md).
>
> Update any bookmarks or links to point to the canonical document. Note: anchor-fragment links cannot be forwarded by this stub — update them directly to `docs/spec/language.md#<anchor>`.

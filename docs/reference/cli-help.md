<!-- DO NOT EDIT — generated by `pnpm run docs:cli-help` from packages/cli/src/cli.ts -->
<!-- To regenerate: pnpm run docs:cli-help -->
<!-- To verify drift: pnpm run check:docs:cli-help -->

# rundown CLI help

Faithful projection of the Rundown CLI surface, generated from the running
Commander program. For the user guide, see [cli.md](cli.md).

## Commands

- [`rundown`](#rundown)
  - [`rundown run`](#rundown-run)
  - [`rundown pass`](#rundown-pass)
  - [`rundown fail`](#rundown-fail)
  - [`rundown complete`](#rundown-complete)
  - [`rundown goto`](#rundown-goto)
  - [`rundown status`](#rundown-status)
  - [`rundown stop`](#rundown-stop)
  - [`rundown ls`](#rundown-ls)
  - [`rundown stash`](#rundown-stash)
  - [`rundown pop`](#rundown-pop)
  - [`rundown echo`](#rundown-echo)
  - [`rundown check`](#rundown-check)
  - [`rundown resolve`](#rundown-resolve)
  - [`rundown prune`](#rundown-prune)
  - [`rundown prompt`](#rundown-prompt)
  - [`rundown scenario`](#rundown-scenario)
    - [`rundown scenario ls`](#rundown-scenario-ls)
    - [`rundown scenario show`](#rundown-scenario-show)
    - [`rundown scenario run`](#rundown-scenario-run)
  - [`rundown scenario-suite`](#rundown-scenario-suite)
    - [`rundown scenario-suite ls`](#rundown-scenario-suite-ls)
    - [`rundown scenario-suite show`](#rundown-scenario-suite-show)
    - [`rundown scenario-suite run`](#rundown-scenario-suite-run)
  - [`rundown delegate`](#rundown-delegate)
  - [`rundown claim`](#rundown-claim)
  - [`rundown abort`](#rundown-abort)
  - [`rundown collect`](#rundown-collect)
  - [`rundown artifact`](#rundown-artifact)
    - [`rundown artifact ls`](#rundown-artifact-ls)
    - [`rundown artifact path`](#rundown-artifact-path)
    - [`rundown artifact uri`](#rundown-artifact-uri)
    - [`rundown artifact inspect`](#rundown-artifact-inspect)

## `rundown`

```text
Usage: rundown [options] [command]

Runbook orchestration CLI

Options:
  -V, --version                 output the version number
  --no-color                    Disable colored output
  --schema                      Output JSON schema for the command's JSON output
  -h, --help                    display help for command

Policy options:
  --allow-run <commands>        Allow specific commands (comma-separated)
  --allow-read <paths>          Allow reading specific paths (comma-separated)
  --allow-write <paths>         Allow writing to specific paths
                                (comma-separated)
  --allow-env <vars>            Allow specific environment variables
                                (comma-separated)
  --allow-all                   Allow all operations (bypass policy)
  --deny-all                    Deny all operations
  --policy <file>               Path to policy configuration file
  --trust-js-policy             Trust executable JavaScript policy config files
  -y, --yes                     Skip confirmation prompts
  --non-interactive             Non-interactive mode (no prompts, CI-friendly)
  --sandbox                     Enable OS-level sandbox for file access
                                enforcement
  --no-sandbox                  Disable sandbox enforcement (trust mode); the
                                explicit opt-out when the sandbox is unavailable
  --sandbox-strict              Fail if sandbox is unavailable (default;
                                explicit affirmation of fail-closed)
  --helpers <paths>             Helper module paths to load (comma-separated,
                                relative to project root)

Commands:
  run [options] [file]          Start a runbook
  pass|yes [options]            Mark current step as passed (triggers PASS
                                transition)
  fail|no [options]             Mark current step as failed (triggers FAIL
                                transition)
  complete [options] [message]  Force early completion of current runbook
                                (runbooks auto-complete on final step)
  goto [options] <step>         Jump to specific step (e.g., "3" or "3.1" for
                                substep)
  status [options]              Show current runbook state
  stop [options] [message]      Abort current runbook
  ls [options]                  List runbooks (active by default, --all for
                                available)
  stash [options]               Pause runbook enforcement, preserve state
  pop [options]                 Resume enforcement from stashed runbook
  echo [options] [command...]   Echo command for runbook testing
  check [options] <file>        Check a runbook file for errors
  resolve [options] <file>      Resolve and validate runbook variables and data
                                sources
  prune [options]               Remove runbook state (does not delete runbook
                                files)
  prompt [options] <content>    Output content wrapped in markdown fences
  scenario                      List, show, or run scenarios from a runbook
  scenario-suite                List, show, or run cases from a scenario suite
                                file
  delegate [options] [runbook]  Create a delegation token for a child runbook
  claim [options] <token>       Claim a delegation token and launch the child
                                runbook
  abort [options] <token>       Cancel a delegation token
  collect [options]             Collect delegation results and fire aggregation
                                transition
  artifact                      Inspect Rundown artifact aliases
  help [command]                display help for command
```

### `rundown run`

```text
Usage: rundown run [options] [file]

Start a runbook

Options:
  --prompted                   Prompted mode: show commands without
                               auto-executing
  --step <stepId>              Link child to parent substep (or jump to step
                               with --prompted)
  --index <number>             FOR loop iteration to target (requires --step)
  --text                       Output execution events as human-readable text
  -h, --help                   display help for command

Input options:
  --input-file <path>          Load inputs from YAML file (repeatable) (default:
                               [])
  --input <key=value>          Set input (repeatable, omit =value to inherit
                               from env) (default: [])
  --input-json <key=json>      Set input with JSON value (repeatable) (default:
                               [])
  --artifacts <key=uri>        Supply an input artifact by rd:// URI
                               (repeatable) (default: [])
  --artifacts-json <key=json>  Supply input artifacts as a JSON array of rd://
                               URIs (repeatable) (default: [])
```

### `rundown pass`

```text
Usage: rundown pass|yes [options]

Mark current step as passed (triggers PASS transition)

Options:
  --step <stepId>       Target specific substep
  --index <number>      FOR loop iteration to target (requires --step)
  --claim-id <claimId>  Target a claimed delegated child runbook
  --run <runId>         Name the run you control (explicit orchestrator
                        targeting)
  --text                Output as human-readable text
  -h, --help            display help for command
```

### `rundown fail`

```text
Usage: rundown fail|no [options]

Mark current step as failed (triggers FAIL transition)

Options:
  --step <stepId>       Target specific substep
  --index <number>      FOR loop iteration to target (requires --step)
  --claim-id <claimId>  Target a claimed delegated child runbook
  --run <runId>         Name the run you control (explicit orchestrator
                        targeting)
  --text                Output as human-readable text
  -h, --help            display help for command
```

### `rundown complete`

```text
Usage: rundown complete [options] [message]

Force early completion of current runbook (runbooks auto-complete on final step)

Arguments:
  message               Completion message

Options:
  --claim-id <claimId>  Target a claimed delegated child runbook
  --run <runId>         Name the run you control (explicit orchestrator
                        targeting)
  --text                Output as human-readable text
  -h, --help            display help for command
```

### `rundown goto`

```text
Usage: rundown goto [options] <step>

Jump to specific step (e.g., "3" or "3.1" for substep)

Options:
  --index <number>      FOR loop iteration to target
  --claim-id <claimId>  Target a claimed delegated child runbook
  --run <runId>         Name the run you control (explicit orchestrator
                        targeting)
  --text                Output as human-readable text
  -h, --help            display help for command
```

### `rundown status`

```text
Usage: rundown status [options]

Show current runbook state

Options:
  --claim-id <claimId>  Target a claimed delegated child runbook
  --text                Output as human-readable text
  -h, --help            display help for command
```

### `rundown stop`

```text
Usage: rundown stop [options] [message]

Abort current runbook

Arguments:
  message               Stop message

Options:
  --claim-id <claimId>  Target a claimed delegated child runbook
  --run <runId>         Name the run you control (explicit orchestrator
                        targeting)
  --text                Output as human-readable text
  -h, --help            display help for command
```

### `rundown ls`

```text
Usage: rundown ls [options]

List runbooks (active by default, --all for available)

Options:
  -a, --all      List all available runbook files
  --text         Output as human-readable text
  --tags <tags>  Filter available runbooks by comma-separated tags
  -h, --help     display help for command
```

### `rundown stash`

```text
Usage: rundown stash [options]

Pause runbook enforcement, preserve state

Options:
  --claim-id <claimId>  Target a claimed delegated child runbook
  --text                Output as human-readable text
  -h, --help            display help for command
```

### `rundown pop`

```text
Usage: rundown pop [options]

Resume enforcement from stashed runbook

Options:
  --claim-id <claimId>  Target a claimed delegated child runbook
  --text                Output as human-readable text
  -h, --help            display help for command
```

### `rundown echo`

```text
Usage: rundown echo [options] [command...]

Echo command for runbook testing

Options:
  -r, --result <outcome>  Add result to sequence (pass|fail) (default: [])
  --text                  Output as human-readable text
  -h, --help              display help for command
```

### `rundown check`

```text
Usage: rundown check [options] <file>

Check a runbook file for errors

Options:
  --text      Output as human-readable text
  -h, --help  display help for command
```

### `rundown resolve`

```text
Usage: rundown resolve [options] <file>

Resolve and validate runbook variables and data sources

Input options:
  --input-file <path>          Load inputs from YAML file (repeatable) (default:
                               [])
  --input <key=value>          Set input (repeatable, omit =value to inherit
                               from env) (default: [])
  --input-json <key=json>      Set input with JSON value (repeatable) (default:
                               [])
  --artifacts <key=uri>        Supply an input artifact by rd:// URI
                               (repeatable) (default: [])
  --artifacts-json <key=json>  Supply input artifacts as a JSON array of rd://
                               URIs (repeatable) (default: [])

Options:
  --text                       Output as human-readable text
  -h, --help                   display help for command
```

### `rundown prune`

```text
Usage: rundown prune [options]

Remove runbook state (does not delete runbook files)

Options:
  --dry-run    Show what would be removed without deleting
  --completed  Prune successfully completed runbook state
  --stopped    Prune stopped (aborted/failed) runbook state
  --active     Prune active runbook state
  --inactive   Prune inactive (orphaned) runbook state
  --all        Prune all runbook state
  --text       Output as human-readable text
  -h, --help   display help for command
```

### `rundown prompt`

```text
Usage: rundown prompt [options] <content>

Output content wrapped in markdown fences

Options:
  --text      Output as human-readable text
  -h, --help  display help for command
```

### `rundown scenario`

```text
Usage: rundown scenario [options] [command]

List, show, or run scenarios from a runbook

Options:
  -h, --help                    display help for command

Commands:
  ls [options] <file>           List all scenarios in a runbook
  show [options] <file> <name>  Show details for a specific scenario
  run [options] <file> <name>   Execute a scenario and verify the result
  help [command]                display help for command
```

#### `rundown scenario ls`

```text
Usage: rundown scenario ls [options] <file>

List all scenarios in a runbook

Options:
  --text      Output as human-readable text
  -h, --help  display help for command
```

#### `rundown scenario show`

```text
Usage: rundown scenario show [options] <file> <name>

Show details for a specific scenario

Options:
  --text      Output as human-readable text
  -h, --help  display help for command
```

#### `rundown scenario run`

```text
Usage: rundown scenario run [options] <file> <name>

Execute a scenario and verify the result

Options:
  -q, --quiet  Suppress command output
  --text       Output as human-readable text
  -h, --help   display help for command
```

### `rundown scenario-suite`

```text
Usage: rundown scenario-suite [options] [command]

List, show, or run cases from a scenario suite file

Options:
  -h, --help                          display help for command

Commands:
  ls [options] <suite-file>           List all cases in a scenario suite
  show [options] <suite-file> <case>  Show details for a specific case in a
                                      suite
  run [options] <suite-file> [case]   Execute a case (or all cases with --all)
                                      from a suite
  help [command]                      display help for command
```

#### `rundown scenario-suite ls`

```text
Usage: rundown scenario-suite ls [options] <suite-file>

List all cases in a scenario suite

Options:
  --text      Output as human-readable text
  -h, --help  display help for command
```

#### `rundown scenario-suite show`

```text
Usage: rundown scenario-suite show [options] <suite-file> <case>

Show details for a specific case in a suite

Options:
  --text      Output as human-readable text
  -h, --help  display help for command
```

#### `rundown scenario-suite run`

```text
Usage: rundown scenario-suite run [options] <suite-file> [case]

Execute a case (or all cases with --all) from a suite

Options:
  --all        Run all cases in the suite
  -q, --quiet  Suppress command output
  --text       Output as human-readable text
  -h, --help   display help for command
```

### `rundown delegate`

```text
Usage: rundown delegate [options] [runbook]

Create a delegation token for a child runbook

Options:
  --step <stepId>              Step to delegate (e.g., 1.1 or 1.2.1 for
                               step.iteration.substep)
  --index <number>             FOR loop iteration to target (requires --step)
  --retry                      Retry an existing delegation: cancel and re-issue
                               with a fresh token
  --run <runId>                Name the run you control (explicit orchestrator
                               targeting)
  --text                       Output as human-readable text
  -h, --help                   display help for command

Input options:
  --input <key=value>          Set input for child context (repeatable, omit
                               =value to inherit from env) (default: [])
  --input-json <key=json>      Set input with JSON value (repeatable) (default:
                               [])
  --input-file <path>          Load inputs from YAML file (repeatable) (default:
                               [])
  --artifacts <key=uri>        Supply an input artifact by rd:// URI
                               (repeatable) (default: [])
  --artifacts-json <key=json>  Supply input artifacts as a JSON array of rd://
                               URIs (repeatable) (default: [])
```

### `rundown claim`

```text
Usage: rundown claim [options] <token>

Claim a delegation token and launch the child runbook

Options:
  --text                       Output as human-readable text
  -h, --help                   display help for command

Input options:
  --input-file <path>          Load inputs from YAML file (repeatable) (default:
                               [])
  --input <key=value>          Set input (repeatable, omit =value to inherit
                               from env) (default: [])
  --input-json <key=json>      Set input with JSON value (repeatable) (default:
                               [])
  --artifacts <key=uri>        Supply an input artifact by rd:// URI
                               (repeatable) (default: [])
  --artifacts-json <key=json>  Supply input artifacts as a JSON array of rd://
                               URIs (repeatable) (default: [])
```

### `rundown abort`

```text
Usage: rundown abort [options] <token>

Cancel a delegation token

Options:
  --force     Force cancel even if delegation is claimed (stops child run)
  --text      Output as human-readable text
  -h, --help  display help for command
```

### `rundown collect`

```text
Usage: rundown collect [options]

Collect delegation results and fire aggregation transition

Options:
  --step <stepId>       Target specific DELEGATE step scope (e.g., "1" or "1.2")
  --index <number>      FOR loop iteration to target (requires --step on a FOR
                        step)
  --claim-id <claimId>  Target a claimed delegated child runbook
  --run <runId>         Name the run you control (explicit orchestrator
                        targeting)
  --text                Output as human-readable text
  -h, --help            display help for command
```

### `rundown artifact`

```text
Usage: rundown artifact [options] [command]

Inspect Rundown artifact aliases

Options:
  -h, --help                        display help for command

Commands:
  ls [options]
  path [options] <alias-or-uri>
  uri [options] <alias>
  inspect [options] <alias-or-uri>
  help [command]                    display help for command
```

#### `rundown artifact ls`

```text
Usage: rundown artifact ls [options]

Options:
  --text      Output as human-readable text
  -h, --help  display help for command
```

#### `rundown artifact path`

```text
Usage: rundown artifact path [options] <alias-or-uri>

Options:
  --text      Output as human-readable text
  -h, --help  display help for command
```

#### `rundown artifact uri`

```text
Usage: rundown artifact uri [options] <alias>

Options:
  --text      Output as human-readable text
  -h, --help  display help for command
```

#### `rundown artifact inspect`

```text
Usage: rundown artifact inspect [options] <alias-or-uri>

Options:
  --text      Output as human-readable text
  -h, --help  display help for command
```
